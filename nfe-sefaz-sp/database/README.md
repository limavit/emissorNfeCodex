# Database

As migrations executadas pelo backend ficam em `backend/src/main/resources/db/migration`.

Esta pasta documenta o contrato de banco no monorepo e guarda seeds ficticios para desenvolvimento.
